{
  "name": "moveit",
  "version": "1.0.0",
  "description": "Move-It Logistics Application",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd backend && npm start"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
